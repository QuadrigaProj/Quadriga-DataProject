# 서버 실행에 필요한 것만. 배포(Render)가 이 파일을 씁니다.
# 분석·노트북용 도구는 requirements-dev.txt 에 있습니다.

# API 서버
fastapi
uvicorn[standard]
httpx                    # OAuth 토큰 교환 · TestClient

# 데이터 처리 (체력나이 산출)
pandas
numpy

# DB — 로컬은 SQLite(표준 라이브러리), 배포는 Postgres
psycopg[binary]

# AI 루틴 추천 (선택) — ANTHROPIC_API_KEY 가 없으면 부르지 않고 점수만으로 추천합니다
anthropic

# 테스트
pytest
